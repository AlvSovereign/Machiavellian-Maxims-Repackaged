import { Maxim } from './maxim.model';

const controllers = {
  userUser: async (req, res) => {
    const { username, password } = req.body;

    try {
    } catch (err) {}
  }
};

export default controllers;
